/target
/data
*.db
*.db-*
node_modules/
dist/
.DS_Store

# test-only secrets — never commit
.env
.env.*

# ── Deno Deploy ──
.deno_cache

# ── wasm-bindgen build output (regenerated; never commit) ──
pkg/